# PodSleuth Agent Notes

- Keep analysis read-only by default.
- Treat missing IAM trust relationships as uncertainty, not access.
- Preserve evidence for every finding and rendered explanation.
- Prefer deterministic JSON over terminal-only summaries.
